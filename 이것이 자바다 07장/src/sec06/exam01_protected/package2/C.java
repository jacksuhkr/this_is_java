package sec06.exam01_protected.package2;
import sec06.exam01_protected.package1.A;

public class C {
	public void method() {
//		A a = new A();		// (x) 자식 클래스가 아니라서
//		a.field = "value";	// (x) protected에 접근할 수 없다
//		a.method();			// (x)
	}
}
